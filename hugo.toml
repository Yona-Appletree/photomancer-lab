baseURL = "https://lab.photomancer.art/"
title = "Photomancer Lab"
languageCode = "en"
DefaultContentLanguage = "en"
enableInlineShortcodes = true
enableRobotsTXT = true
footnoteReturnLinkContents = "^"
theme = "hugotex"
ignoreFiles = ["\\.post\\.ts$", "\\.gen\\.test\\.ts$"]

[pagination]
  pagerSize = 10

[Params.author]
  name = "Yona Appletree"
  abstract = """
  Notes on software, light, and systems — engineering, photography, and hardware.
  """

[taxonomies]
  tag = "tags"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.extensions]
      [markup.goldmark.extensions.passthrough]
        enable = true
        [markup.goldmark.extensions.passthrough.delimiters]
          block = [["$$", "$$"]]
          inline = [["$", "$"]]
  [markup.highlight]
    style = "paraiso-dark"

[Params]
  darkmode = true
  description = "Notes by Yona Appletree on TypeScript, software architecture, systems, photography, and hardware."
  opengraph = true
  sourceURL = "https://github.com/Yona-Appletree/photomancer-lab"
  twittercard = true
